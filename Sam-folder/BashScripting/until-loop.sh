#!/bin/bash

# untily kataruma nuyn gorcoxutyuny inch while uxaki lriv hakarak
# na katarvuma en depqum ete extentiony Folse e

COUNT=10

until(( ! $COUNT > 0 )); do
    echo "Counter: ${COUNT}"
    (( COUNT -- ))
done
